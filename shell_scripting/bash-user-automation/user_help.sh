#!/bin/bash
    echo "User help manual"

    echo "how to use user_management scripts"
    echo "  user_creation.sh -c, --create    Create a new user"
    echo "  user_deletion.sh -d, --delete    Delete an existing user"
    echo "  user_pass_reset.sh -r, --reset     Reset user password"
    echo "  user_list.sh -l, --list      List all users"
    
